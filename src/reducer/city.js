/* eslint-disable default-case */
import { CITY_DELETE } from '../constants';

export default (cityList = {}, action) => {
  const { type, payLoad } = action;
  switch (type) {
    
  }
  return cityList;
};
